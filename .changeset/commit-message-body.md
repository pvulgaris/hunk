---
"hunkdiff": minor
---

Show the full commit message at the top of the review stream for direct `hunk show` reviews and commits opened from `hunk log`, scrolling away with the diff; commit descriptors gain an optional multi-line `body`, and the session daemon revision advances so mixed-build windows report the skew instead of a bare rejection.
